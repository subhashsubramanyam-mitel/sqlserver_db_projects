CREATE TABLE [enum].[MonthlySubCategory] (
    [NextMonthMRRStatus] NVARCHAR (100) NOT NULL,
    [Id]                 INT            NOT NULL,
    CONSTRAINT [PK_DimMonthlySubCategory_1] PRIMARY KEY CLUSTERED ([Id] ASC)
);

